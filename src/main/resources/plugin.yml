name: ${project.name}
version: ${project.version}
api: ["1.0.8"]
author: The Oni
description: ${project.description}
main: theoni.splitchat.Main

permissions:
  splitchat.chat.local:
    default: op
  splitchat.chat.global:
    default: op
  splitchat.command.spy:
    default: op
